remote_theme: Antho-Instructor/workshop-theme

# disable the jekyll-titles-from-headings plugin,
# so first h1 will not be used as prior title Eof you page
titles_from_headings:
  enabled: false

defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default" # possible values: default or tic-tac

style: agorski # possible values: agorski, dev, data, ciber or design.
title: Group review on React
description: ""
# type: ''
# main_image: '' # http or relative path to image
show_clone: false
show_toc: true
lang: en
